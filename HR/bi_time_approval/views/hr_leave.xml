<odoo>
    <record id="view_hr_leave_form_custom" model="ir.ui.view">
        <field name="name">hr.leave.form.custom</field>
        <field name="model">hr.leave</field>
        <field name="inherit_id" ref="hr_holidays.hr_leave_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[@name='action_approve']" position="attributes">
                <attribute name="groups">bi_time_approval.group_time_off_approval</attribute>
            </xpath>
            <xpath expr="//form" position="inside">
                <field name="is_notify"/>
            </xpath>
            <xpath expr="//header" position="inside">
                <button name="action_notify" type="object" string="Notify" class="btn-primary" invisible="state not in ('confirm') or is_notify"/>
            </xpath>
        </field>
    </record>
</odoo>